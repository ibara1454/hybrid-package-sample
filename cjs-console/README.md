# Console App on CommonJS module system

Usage:

```bash
# Install the dependencies (hybrid package `library`)
npm install
# Or you could install `library` directly
npm install --save ../library
# Print message
npm start
```
